import { NgStickyHeaderDirective } from './ng-sticky-header.directive';

describe('NgStickyHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new NgStickyHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
